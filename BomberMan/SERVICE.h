#ifndef SERVIC_H
#define SERVIC_H

#define ob_size 40
#define area_h 15
#define area_w 31

#define for_each(i) for( int i = 0; i < 7; i++ )
#define for_each(ii) for( int i = 6; i >= 0; i-- )

#include <string>
#include <vector>
#include <GL\glut.h>
using namespace std;

#endif //SERVIC_H