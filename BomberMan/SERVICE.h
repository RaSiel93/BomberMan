#ifndef SERVIC_H
#define SERVIC_H

#define ob_size 40
#define area_h 13
#define area_w 19

#define for_each(i) for( int i = 0; i < 7; i++ )
#define for_each(ii) for( int i = 6; i >= 0; i-- )

#include <setjmp.h>

#include <Windows.h>
#include <string>
#include <vector>
#include <mmsystem.h>
#include <GL\glut.h>

using namespace std;

#endif //SERVIC_H